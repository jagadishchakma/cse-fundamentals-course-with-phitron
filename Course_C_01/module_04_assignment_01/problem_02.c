#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    long long int a, b;
    scanf("%lld %lld", &a, &b);
    long long int mult = a * b;
    printf("%lld", mult);
    
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}