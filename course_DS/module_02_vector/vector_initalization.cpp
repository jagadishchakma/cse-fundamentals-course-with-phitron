#include <bits/stdc++.h>
using namespace std;

int main(){
    // vector<int> v; // vector type 1;
    // cout << v.size();
    // vector<int> v(5); // vector type 2;
    // vector<int> v(5,19); // vector type 3;
    // vector<int> v2(12,23);
    // vector<int> v = v2; // vector type 4
    // int a[] = {1,2,3,4,5};
    // vector<int> v(a,a+6); // vector type 5;
    vector<int> v = {1,2,3,4,5};
    for (int i = 0; i < v.size(); i++)
    {
        cout << v[i];
    }
    
    return 0;
}